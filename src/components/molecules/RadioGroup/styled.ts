import styled from '@emotion/styled';
import _InputLabel from '../InputLabel';

const Label = styled.span`
  color: rgba(0, 0, 0, 0.6);
`;

const InputLabel = styled(_InputLabel)`
  color: #1a2027;
`;

export default { Label, InputLabel };
